apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: argo-demo-from-configmap
  namespace: declarative-grafana
spec:
  instanceSelector:
    matchLabels:
      dashboards: "pbmoses"
  configMapRef:
    name: dashboard-definition-1
    key: json
